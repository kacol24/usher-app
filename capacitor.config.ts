import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lovinyouforefer.usher',
  appName: 'Usher App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
